# Jupyter Lab bind port
JUPYTER_PORT=8888

# Data source URL for the sample dataset (see notebooks/01_load_data.ipynb).
# Defaults to a UN population CSV mirror; override with your own dataset if needed.
DATA_URL=https://raw.githubusercontent.com/datasets/population/main/data/population.csv
